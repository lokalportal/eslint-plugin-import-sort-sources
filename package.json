{
  "name": "eslint-plugin-import-sort-source",
  "version": "0.0.2",
  "description": "ESLint plugin to check sorting imports after source",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha tests/**/*.js"
  },
  "keywords": [
    "eslint",
    "eslint-plugin",
    "eslintplugin"
  ],
  "license": "MIT",
  "devDependencies": {
    "eslint": "4.19.1",
    "mocha": "5.0.5"
  },
  "peerDependencies": {
    "eslint": "^2.0.0 || ^3.0.0 || ^4.0.0"
  },
  "engines": {
    "node": ">=0.10"
  }
}
